# 目前開發進度與狀態 (Current Status)

**最後更新時間：** 2026-04-01

## 1. 開發狀態

| Phase | 狀態 | 說明 |
|---|---|---|
| Phase 1~5 | ✅ 完成 | POC → 核心引擎 → UI → 整合測試 → 交付封裝 |
| Phase 6 | ✅ 完成 | 範本驅動問卷重構與標記強化 |
| Phase 6b | ✅ 完成 | P2/P3 深層問卷產出機制與動態表單實作 |
| Phase 7 | ✅ 完成 | 測試回饋修復與自動化歸檔模組實作 (範例檔邏輯已就緒) |
| Phase 8 | ✅ 完成 | 企業品牌整合、全局編號同步與隱私聲明 |
| Phase 9 | ✅ 完成 | 系統底層標籤遷移 (qpower) 與客製發布包除錯 |

## 2. 核心里程碑回放 (2026-03-09 更新)

### Phase 7: 修復反饋與進階整理 (v0.7.0)
- [x] 修正標記遺缺：密碼到期天數預設值動態化、IS-004-03 風險值帶入。
- [x] 打包檔目錄體系重構：自動還原 ISO 27001 階層結構 (`Tier 1/2層`、`表單/`、空 `紀錄/`)。
- [x] 產出 `問卷填答總表.docx`，直接掛載於 ZIP 根目錄。
- [x] 開通 `examples/` 例證自動定位：邏輯已就緒，待實體範例檔放入後自動進入 ZIP。
- [x] 清理無用之 `_output_` / `_processed` 等除錯中繼檔案。
- [x] 排版強化：政策(Tier1)/程序(Tier2)之文件封面套用公司名稱 24 級粗體。

### Phase 8: 全局自動化與品牌化 (v0.8.0)
- [x] **品牌重塑**：導入「諮力管理顧問」主視覺與隱私保護聲明區塊。
- [x] **全局編號同步**：實作自訂前綴 (IS -> 自定義)，同步修改目錄、檔名及全文件(Word/Excel)內文編號。
- [x] **ISMS 範圍動態化**：IS-001 營運範圍描述支援問卷產出。

### Phase 9: 系統底層標籤遷移與客製發布包除錯 (v0.9.0)
- [x] **qpower 標籤遷移**：掃描並修改所有實體範本檔名及內部 Word/Excel 內容，將系統底層與預設前綴由 `IS-` 強制重構為 `qpower-`。
- [x] **範例檔解密處理**：修復了產生客戶交付發布檔時，不規則名稱或加密範例發生例外跳脫，導致沒有正確解密 `.enc` 的嚴苛錯誤。

## 3. 測試狀況
* 11/11 E2E 測試全數通過 ✅ (包含生成、文字替代、Excel修改、排版強化與全局編號替換)
* (03/09) 已修復 Word 段落跨 Run 分裂導致的 `IS-XXX` 內文前綴未正確替換之錯誤。
* (03/09) 已通過擴充版 `examples/` 壓力測試：30 份包含各式不規則後綴名（如 `_範例`, `(範例)` 等）的檔案，皆能被清洗為標準的 `-範例`，並隨文件同步前綴與精準投入「四階層目錄架構」中。
* (03/09) 修復 `python-docx` 頁首/頁尾 (Header/Footer) 內文及表格之前綴同步失效問題，改以 Double-pass 存檔替換機制完整解決。
* (04/01) 已全面將系統底層預設範本與設定轉換為 qpower 前綴，並修復了客戶交付包內 `.enc` 範例檔未被解密的錯誤。